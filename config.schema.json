{
  "pluginAlias": "TahomaPortail",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Nom de l'accessoire",
        "type": "string",
        "default": "Portail"
      },
      "ip": {
        "title": "Adresse IP de la box Tahoma",
        "type": "string",
        "placeholder": "192.168.1.50:8443"
      },
      "token": {
        "title": "Token d'authentification",
        "type": "string",
        "description": "Collez ici le token généré via l’API Somfy"
      },
      "deviceURL": {
        "title": "Device URL du portail",
        "type": "string",
        "placeholder": "io://1234-4567-8901/12345678"
      },
      "pollingInterval": {
        "title": "Intervalle de mise à jour du portail (secondes)",
        "type": "integer",
        "default": 10,
        "minimum": 5,
        "maximum": 300,
        "description": "Temps entre chaque mise à jour de l'état du portail."
      },
      "logState": {
        "title": "Activer les logs d'état",
        "type": "boolean",
        "default": false
      },
      "logInterval": {
        "title": "Intervalle des logs (secondes)",
        "type": "integer",
        "default": 30,
        "minimum": 5,
        "maximum": 300,
        "description": "Définissez toutes les combien de secondes afficher l'état du portail (utilisé uniquement si 'Activer les logs' est activé)"
      }
    },
    "required": ["ip", "token"]
  }
}